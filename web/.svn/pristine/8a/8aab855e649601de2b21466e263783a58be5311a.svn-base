/**
 * 사용자 권한을 확인하는 함수
 * @param {any} user 사용자 객체
 * @param {string} permission 확인할 권한
 * @returns {boolean} 해당 권한을 가지고 있는지 여부
 */
export function userHasPermission(user, permission) {
    return user.authorities.includes(permission);
  }